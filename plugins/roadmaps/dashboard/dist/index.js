// Roadmaps dashboard UI ships in PR5.
//
// This stub satisfies the manifest ``entry`` (dist/index.js) so the plugin
// loads cleanly behind the dashboard tab system before the React bundle
// lands. The backend REST surface in plugin_api.py is fully functional and
// tested independently.
